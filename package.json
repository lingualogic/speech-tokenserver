{
  "name": "dialogflow-tokenserver",
  "version": "0.5.15",
  "author": "Lingualogic Team",
  "license": "MIT",
  "description": "Dialogflow Tokenserver",
  "main": "server/tokenserver.js",
  "scripts": {
    "postinstall": "gulp install",
    "start": "node server/tokenserver",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "google-oauth-jwt": "^0.2.0"
  },
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-file": "^0.4.0",
    "gulp-inject-string": "^1.1.2",
    "gulp-shell": "^0.7.1",
    "run-sequence": "^2.2.1"
  }
}
